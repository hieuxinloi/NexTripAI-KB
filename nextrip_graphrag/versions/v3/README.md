# KB V3 Target

Entity resolution, confidence-bearing claims, multi-vector place profiles and query-adaptive graph-first retrieval.

Rain recommendations compile to verified hard constraints. V3 only returns places with
`is_indoor=true` or `weather_suitable` containing `all_weather`, and exposes those attributes
with each candidate so downstream synthesis can explain suitability without guessing.
