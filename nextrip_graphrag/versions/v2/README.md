# KB V2 Target

Typed travel ontology, `SourceRecord`/`EvidenceChunk` provenance, Place profile and evidence chunk embeddings, cache/resume ingestion, hybrid anchor retrieval, graph traversal and constraint-aware reranking.

Status: planned. Retrieval-only changes on the V1 graph must not be placed here.
