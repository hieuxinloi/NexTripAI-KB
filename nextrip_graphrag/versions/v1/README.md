# KB V1 Baseline

Current `Place`/`Term` ontology, one Place search text embedding unit, vector-first retrieval and keyword fallback. Implementation currently lives in the legacy shared modules and is frozen for comparison.
